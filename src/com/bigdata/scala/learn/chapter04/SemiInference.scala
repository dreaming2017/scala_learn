package com.bigdata.scala.learn.chapter04

object SemiInference {
  def printProd2() {
    val s = "hello"; println(s)
  }

  def check(x: Int) {
    if (x < 2)
      println("too small")
    else
      println("ok")
  }
}
